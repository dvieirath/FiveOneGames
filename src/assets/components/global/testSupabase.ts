import { supabase } from '../../../../supabaseClient'; // Caminho correto para raiz

// Para rodar este arquivo, utilize:
// npx ts-node --loader ts-node/esm src/assets/components/global/testSupabase.ts

// Certifique-se que o tsconfig.json tem "module": "commonjs" ou "esnext".

// Arquivo removido automaticamente por limpeza do projeto.